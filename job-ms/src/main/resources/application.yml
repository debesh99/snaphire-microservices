server:
  port: 8082

spring:
  application:
    name: job-ms
  datasource:
    url: jdbc:postgresql://localhost:5433/snaphire_job_db
    username: postgres
    password: Dp99@gmail
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/